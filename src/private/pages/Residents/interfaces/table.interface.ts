export interface PaginationInfo {
  current: number;
  pageSize: number;
}
export interface DataType {
  key: React.Key;
  name: string;
  private: string;
  numberHouse: number;
  phone: string;
  email: string;
}